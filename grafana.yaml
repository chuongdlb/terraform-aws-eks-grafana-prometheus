datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
    - name: Prometheus
      type: prometheus
      url: http://prometheus-eks-server.lens-metrics.svc.cluster.local
      access: proxy
      isDefault: true
